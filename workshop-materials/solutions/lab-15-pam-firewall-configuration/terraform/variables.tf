# variables.tf for Lab 15: PAM Implementation and Firewall Configuration

variable "network" {
  description = "The name of the network to apply firewall rules to."
  type        = string
}

# Add more variables as needed for PAM and firewall configuration
